<Page
    x:Class="Broccoli.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Broccoli"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="150" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Panel izquierdo -->
        <Grid 
            Grid.Column="0" 
              Background="LightGray">
            <TextBlock 
                Text="Panel Izquierdo" 
                HorizontalAlignment="Center" 
                VerticalAlignment="Center" />
        </Grid>

        <!-- GridSplitter para estirar los paneles -->
        <SplitView Grid.Column="1" Width="5" Background="Gray" HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch" />

        <!-- Panel derecho -->
        <Grid Grid.Column="2" Background="LightBlue">
            <TextBlock Text="Panel Derecho" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Page>